---
phase: 01-m0-bootstrap-h-t-ng
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - prisma/.gitignore
  - prisma/seed/users.ts
  - prisma/seed/organizations.ts
  - prisma/seed/helpers.ts
autonomous: true
requirements:
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Schema Prisma có đầy đủ bảng cho M0: User, Role, Permission, UserRole, RolePermission, Organization, Attachment (polymorphic), AuditLog"
    - "Schema có scaffolding (placeholder model) cho ProgramCycle/Project/Council để phase sau có ground để mở rộng — KHÔNG để rỗng để tránh refactor schema schema lớn ở Phase 2-3"
    - "Database SQLite đã được push schema (`prisma db push` thành công, generate prisma client thành công)"
    - "8 tài khoản hardcoded đã seed với bcrypt cost 10, password match đúng theo CLAUDE.md §5"
    - "3 organization scaffold đã seed: Cục XTTM (CUC_XTTM), Bộ Công Thương (BO_CT), VITAS, LEFASO, VINATEX (real Vietnamese org names)"
    - "Seed script idempotent — chạy lại không nhân bản record"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema với SQLite provider, datasource, generator client, models cho M0"
      contains: "model User"
    - path: "prisma/seed.ts"
      provides: "Entry point seed idempotent, hash password bcrypt cost 10, upsert by username/code"
      contains: "bcryptjs"
    - path: "prisma/seed/users.ts"
      provides: "Seed 8 hardcoded users với bcrypt hash"
      contains: "HARDCODED_USERS"
    - path: "prisma/seed/organizations.ts"
      provides: "Seed 5 organizations với real Vietnamese names"
      contains: "VITAS"
    - path: "prisma/dev.db"
      provides: "SQLite database file (gitignored, generated by prisma db push)"
      contains: ""
  key_links:
    - from: "prisma/seed.ts"
      to: "lib/constants.ts HARDCODED_USERS"
      via: "import for seed input"
      pattern: "HARDCODED_USERS"
    - from: "prisma/seed.ts"
      to: "bcryptjs hash"
      via: "password hashing cost 10"
      pattern: "bcrypt.*hash.*10"
    - from: "prisma/schema.prisma"
      to: "lib/prisma.ts"
      via: "generated @prisma/client"
      pattern: "model User"
---

<objective>
Định nghĩa Prisma schema cho M0 + push lên SQLite + seed 8 tài khoản hardcoded với bcrypt + seed 5 organization với tên đơn vị thực tế. Đây là foundation data layer cho mọi phase sau — schema lock đầy đủ ở M0 để Phase 2-11 chỉ extend, không refactor.

Purpose:
- Khóa schema sớm tránh schema thrashing khi vào Phase 2 (M1) và Phase 3 (M2.1) — pitfall research §4.2 (parentProjectId), §4.1 (state machine fields)
- Cung cấp 8 tài khoản hardcoded đúng theo CLAUDE.md §5 cho Plan 03 (NextAuth) authenticate
- Cung cấp 3-5 org real name cho Phase 4 (M2.2) bắt đầu wire OrgProfile mà không cần re-seed
- AUTH-02 satisfied: bcrypt password hash cost 10 (POC standard, không weak)

Output:
- `prisma/schema.prisma` ~250 dòng với generator + datasource + 8 model M0 (User, Role, Permission, UserRole, RolePermission, Organization, Attachment, AuditLog) + scaffolding placeholder cho 6 entity nghiệp vụ (ProgramCycle, Project, OrgProfile, EvaluationCouncil, Contract, Report)
- `prisma/dev.db` đã được tạo bằng `npx prisma db push`
- `prisma/seed.ts` + `prisma/seed/{users,organizations,helpers}.ts` chạy `npm run db:seed` thành công, idempotent
- `prisma/.gitignore` exclude `dev.db`, `dev.db-journal`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@.planning/phases/01-m0-bootstrap-h-t-ng/01-01-repo-init-PLAN.md
</context>

<interfaces>
<!-- Plan 02 produces these for Plan 03 (NextAuth) and downstream phases -->

```prisma
// prisma/schema.prisma — KEY MODELS the executor MUST define

model User {
  id              String    @id @default(cuid())
  username        String    @unique
  passwordHash    String
  fullName        String
  email           String?
  phone           String?
  role            String    // ADMIN | BANQL | CHUYENVIEN | HOIDONG | DONVI | TAICHINH | LANHDAO
  isActive        Boolean   @default(true)
  organizationId  String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  organization    Organization? @relation(fields: [organizationId], references: [id])
  // Future: scoreSheets, auditLogs, notifications, projects (reverse relations from Phase 2+)

  @@index([organizationId])
  @@index([role])
}

model Organization {
  id        String   @id @default(cuid())
  code      String   @unique  // CUC_XTTM | BO_CT | VITAS | LEFASO | VINATEX
  name      String
  type      String   // ASSOCIATION | RESEARCH_INSTITUTE | ENTERPRISE | GOVERNMENT | OTHER
  taxCode   String?
  address   String?
  phone     String?
  email     String?
  website   String?
  isInvited Boolean  @default(false)
  searchKey String   // diacritics-removed name for VN search
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  users     User[]
  // Future: projects, contracts, profile (reverse relations from Phase 4+)
}
```

For Plan 03 (NextAuth Credentials Provider) the executor will:
- Query `prisma.user.findUnique({ where: { username }, include: { organization: true } })`
- Compare `bcrypt.compare(input.password, user.passwordHash)`
- Return `{ id, username, fullName, role, organizationId, organizationName: user.organization?.name ?? null }`
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed input → DB | `HARDCODED_USERS` từ `lib/constants.ts` chứa plaintext password — chỉ dùng dev/seed, KHÔNG lộ ra prod |
| App → Prisma → SQLite | DB file local, contains bcrypt password hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | Password storage | mitigate | Password MUST hash bằng `bcryptjs.hash(plain, 10)` (cost 10 — chuẩn industry POC). Schema field `passwordHash`, KHÔNG lưu plaintext. Verify bằng grep `prisma/dev.db` (binary, no plaintext password visible). Verify seed.ts không có `password:` key gán string vào schema. |
| T-02-02 | I | Hardcoded user passwords trong source | accept | POC scope theo CLAUDE.md §5; passwords (`Donvi@123` etc.) là demo credentials đã được khách hàng đồng ý mock. KHÔNG dùng cho production. Plan 11 (M7 polish) sẽ bao gồm warning trong README rằng tài khoản hardcoded chỉ dành cho demo. |
| T-02-03 | I | DB file commit | mitigate | `prisma/.gitignore` MUST exclude `dev.db`, `dev.db-journal`, `*.db`, `*.db-journal`. Verify bằng `git ls-files prisma/ \| grep -E '\.db$'` returns empty. |
| T-02-04 | T (Tampering) | Schema integrity | mitigate | Database integrity dựa Prisma migration. Phase 1 dùng `db push` (POC, fast iteration). Phase 11 production migration sẽ chuyển sang `prisma migrate dev`. Schema có FK constraints + cascading delete để prevent orphan. |
| T-02-05 | E | Role hardcoded as string | accept | POC dùng `role` field type String thay vì Prisma enum (do Prisma 6.2+ hỗ trợ enum SQLite nhưng để keep schema simple cho POC + dễ swap to Postgres production). RBAC enforcement ở `lib/permissions.ts` (Plan 01) authoritative — chỗ duy nhất check role. |
| T-02-06 | I | Email/phone trong seed | accept | Seed users dùng email gov VN (xttm.gov.vn, moit.gov.vn) là placeholder mock — không phải thông tin thật của cá nhân. Tên trong fullName là tên VN chung chung, không reference cá nhân thật. |
| T-02-07 | D | Seed runs forever / infinite loop | mitigate | `seed.ts` MUST có `process.exit(0)` ở finally + max execution time < 10s cho 8 users + 5 orgs. Idempotent dùng `upsert` không `create` để tránh unique constraint violation khi rerun. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Định nghĩa Prisma schema cho M0 với scaffolding cho phase sau</name>
  <files>prisma/schema.prisma, prisma/.gitignore</files>
  <read_first>
    - d:/Thaodnp/XTTM/.planning/research/ARCHITECTURE.md §3 (đặc biệt §3.2 schema sketch — top 14 entities)
    - d:/Thaodnp/XTTM/.planning/research/PITFALLS.md §4.1 (state machine), §4.2 (parentProjectId 2-year project)
    - d:/Thaodnp/XTTM/.planning/research/STACK.md §3 (Prisma 6 SQLite — JSON + Enum support)
    - d:/Thaodnp/XTTM/CLAUDE.md mục 4 (7 vai trò), 5 (8 tài khoản), 6 (14 module nghiệp vụ)
    - d:/Thaodnp/XTTM/lib/constants.ts (đã tạo Plan 01 — verify ROLES + ORG_CODES + SLA_THRESHOLDS)
    - d:/Thaodnp/XTTM/lib/workflows/ (đã tạo Plan 01 — verify status enum names match schema)
  </read_first>
  <action>
**Bước 1: Init Prisma trong project (KHÔNG dùng `prisma init` vì nó tạo .env mặc định overlap với Plan 01):**

Tạo `prisma/schema.prisma` thủ công với generator + datasource pointing đến `file:./dev.db`:

```prisma
// XTTMQG — Hệ thống Quản lý Chương trình Quốc gia về Xúc tiến Thương mại
// Schema lock cho M0 + scaffolding cho phase 2-11
// Theo .planning/research/ARCHITECTURE.md §3

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// =============================================================================
// AUTH & RBAC — M0 (AUTH-02)
// =============================================================================

model User {
  id              String   @id @default(cuid())
  username        String   @unique
  passwordHash    String
  fullName        String
  email           String?
  phone           String?
  role            String   // ADMIN | BANQL | CHUYENVIEN | HOIDONG | DONVI | TAICHINH | LANHDAO
  isActive        Boolean  @default(true)
  organizationId  String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  organization    Organization? @relation(fields: [organizationId], references: [id])
  scoreSheets     ScoreSheet[]
  auditLogs       AuditLog[]
  uploadedFiles   Attachment[]  @relation("AttachmentUploader")

  @@index([organizationId])
  @@index([role])
}

// Permissions managed at app layer via lib/permissions.ts MATRIX.
// These models scaffold M1 role/permission matrix UI (Phase 2) so schema doesn't refactor.
model Role {
  id          String           @id @default(cuid())
  code        String           @unique  // ADMIN | BANQL | CHUYENVIEN | HOIDONG | DONVI | TAICHINH | LANHDAO
  name        String
  description String?
  isSystem    Boolean          @default(true)
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  permissions RolePermission[]
}

model Permission {
  id       String           @id @default(cuid())
  code     String           @unique  // resource:action  e.g. "de-an:create"
  resource String
  action   String
  name     String

  roles    RolePermission[]
}

model RolePermission {
  roleId       String
  permissionId String
  granted      Boolean    @default(true)

  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@id([roleId, permissionId])
}

// =============================================================================
// ORGANIZATION — M0 partial (3-5 seed orgs); full M2.2 (AUTH-02 needs Cục XTTM)
// =============================================================================

model Organization {
  id        String   @id @default(cuid())
  code      String   @unique  // CUC_XTTM | BO_CT | VITAS | LEFASO | VINATEX | ...
  name      String
  type      String   // ASSOCIATION | RESEARCH_INSTITUTE | ENTERPRISE | GOVERNMENT | OTHER
  taxCode   String?
  address   String?
  phone     String?
  email     String?
  website   String?
  isInvited Boolean  @default(false)
  searchKey String   // diacritics-removed cho Vietnamese search
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  users     User[]
  profile   OrganizationProfile?
  projects  Project[]
  contracts Contract[]

  @@index([type])
  @@index([searchKey])
}

// Scaffolding cho Phase 4 (M2.2) — full fields populated then.
model OrganizationProfile {
  id              String       @id @default(cuid())
  organizationId  String       @unique
  legalRepName    String?
  legalRepTitle   String?
  capabilities    String?      // năng lực, thành tích
  contactPersons  String?      // JSON string: [{name, title, phone, email, role}]
  status          String       @default("DRAFT")  // DRAFT | SUBMITTED | APPROVED | REJECTED
  submittedAt     DateTime?
  approvedAt      DateTime?
  approvedById    String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt

  organization    Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
}

// =============================================================================
// PROGRAM CYCLE — Scaffold for Phase 3 (M2.1 HERO)
// =============================================================================

model ProgramCycle {
  id                   String              @id @default(cuid())
  year                 Int                 @unique  // 1 chu kỳ / năm
  name                 String
  status               String              @default("DRAFT") // DRAFT | READY | OPEN_REGISTRATION | CLOSED_REGISTRATION | EVALUATING | APPROVED | COMPLETED
  totalBudget          Float?              // VND, SQLite REAL
  registrationOpenAt   DateTime?
  registrationCloseAt  DateTime?           // mặc định 30/5
  evaluationStartAt    DateTime?
  evaluationEndAt      DateTime?
  approvalDeadline     DateTime?
  scanDocumentUrl      String?
  emailTemplateId      String?
  invitedOrganizations String?             // JSON array of organizationIds
  createdById          String?
  createdAt            DateTime            @default(now())
  updatedAt            DateTime            @updatedAt

  projects             Project[]
  councils             EvaluationCouncil[]

  @@index([status])
}

// =============================================================================
// PROJECT — Scaffold for Phase 5 (M2.3 HERO) + Phase 6
// =============================================================================

model Project {
  id                  String           @id @default(cuid())
  code                String           @unique  // XTTM-2026-001
  programCycleId      String
  organizationId      String
  parentProjectId     String?          // Đề án 2 năm — link tới năm 1 (per PITFALLS §4.2)
  name                String
  kind                String           // EXPORT_EXHIBITION | INTL_CONFERENCE | DOMESTIC_FAIR | TRADE_DELEGATION_OUT | TRADE_DELEGATION_IN | TRADE_INFO_EXPORT | TRADE_INFO_DOMESTIC | TRAINING | OTHER
  industrySectorId    String?
  promotionTypeId     String?
  marketIds           String?          // JSON array
  countryIds          String?          // JSON array
  objective           String?
  description         String?
  expectedOutcome     String?
  plannedStartAt      DateTime?
  plannedEndAt        DateTime?
  location            String?
  proposedBudget      Float?
  approvedBudget      Float?
  managerName         String?
  managerTitle        String?
  managerPhone        String?
  managerEmail        String?
  status              String           @default("DRAFT") // see lib/workflows/project.ts
  currentVersion      Int              @default(1)
  submittedAt         DateTime?
  receivedAt          DateTime?
  assignedReviewerId  String?
  searchKey           String           @default("")  // diacritics-removed
  createdById         String?
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  deletedAt           DateTime?

  programCycle        ProgramCycle     @relation(fields: [programCycleId], references: [id])
  organization        Organization     @relation(fields: [organizationId], references: [id])
  parent              Project?         @relation("ProjectYearLink", fields: [parentProjectId], references: [id])
  childProjects       Project[]        @relation("ProjectYearLink")
  scoreSheets         ScoreSheet[]
  contract            Contract?
  reports             Report[]

  @@index([programCycleId, status])
  @@index([organizationId])
  @@index([assignedReviewerId])
  @@index([searchKey])
}

// =============================================================================
// EVALUATION — Scaffold for Phase 7 (M3 HERO)
// =============================================================================

model EvaluationCouncil {
  id              String        @id @default(cuid())
  programCycleId  String
  name            String
  decisionDocUrl  String?
  status          String        @default("DRAFT") // DRAFT | OPEN | CLOSED
  openAt          DateTime?
  closeAt         DateTime?
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  programCycle    ProgramCycle  @relation(fields: [programCycleId], references: [id])
  scoreSheets     ScoreSheet[]
}

model ScoreSheet {
  id           String            @id @default(cuid())
  councilId    String
  projectId    String
  reviewerId   String
  status       String            @default("DRAFT") // DRAFT | SUBMITTED
  scoresJson   String?           // JSON [{criterionId, score, weight, comment}]
  totalScore   Float?
  comment      String?
  conflictOfInterest Boolean      @default(false)
  submittedAt  DateTime?
  createdAt    DateTime          @default(now())
  updatedAt    DateTime          @updatedAt

  council      EvaluationCouncil @relation(fields: [councilId], references: [id])
  project      Project           @relation(fields: [projectId], references: [id])
  reviewer     User              @relation(fields: [reviewerId], references: [id])

  @@unique([councilId, projectId, reviewerId])
}

// =============================================================================
// CONTRACT — Scaffold for Phase 8 (M4)
// =============================================================================

model Contract {
  id              String       @id @default(cuid())
  projectId       String       @unique
  organizationId  String
  contractNo      String       @unique  // XTTM/2026/001
  signedDate      DateTime?
  effectiveFrom   DateTime?
  effectiveTo     DateTime?
  totalValue      Float
  status          String       @default("DRAFT") // DRAFT | SIGNED | ACTIVE | COMPLETED | LIQUIDATED | OVERDUE
  scanFileUrl     String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt

  project         Project      @relation(fields: [projectId], references: [id])
  organization    Organization @relation(fields: [organizationId], references: [id])
}

// =============================================================================
// REPORT — Scaffold for Phase 9 (M5)
// =============================================================================

model Report {
  id               String   @id @default(cuid())
  projectId        String
  title            String
  reportType       String   // PROGRESS | FINAL
  quantitativeData String?  // JSON
  qualitativeData  String?
  status           String   @default("DRAFT") // DRAFT | SUBMITTED | RETURNED | ACCEPTED
  submittedAt      DateTime?
  dueAt            DateTime?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  project          Project  @relation(fields: [projectId], references: [id])
}

// =============================================================================
// ATTACHMENT — Polymorphic, used từ M0+ cho mọi entity
// =============================================================================

model Attachment {
  id           String   @id @default(cuid())
  entityType   String   // Project | Contract | Report | ProgramCycle | OrganizationProfile
  entityId     String
  fileName     String
  fileUrl      String   // path in storage/uploads/ or public/mock-files/
  fileSize     Int?
  mimeType     String?
  uploadedById String
  createdAt    DateTime @default(now())

  uploader     User     @relation("AttachmentUploader", fields: [uploadedById], references: [id])

  @@index([entityType, entityId])
}

// =============================================================================
// AUDIT LOG — M1 scaffolding, write từ M0 (every server action mutation)
// =============================================================================

model AuditLog {
  id         String   @id @default(cuid())
  userId     String
  action     String   // CREATE | UPDATE | DELETE | LOGIN | LOGOUT | TRANSITION | SUBMIT | APPROVE
  resource   String   // Project | ProgramCycle | Contract | Report | User | ...
  resourceId String?
  diffJson   String?  // before/after JSON
  ip         String?
  userAgent  String?
  createdAt  DateTime @default(now())

  user       User     @relation(fields: [userId], references: [id])

  @@index([userId, createdAt])
  @@index([resource, resourceId])
}
```

**Bước 2: Tạo `prisma/.gitignore`:**
```
*.db
*.db-journal
dev.db
dev.db-journal
```

**Lưu ý SQLite + Prisma 6:**
- KHÔNG dùng `Decimal` type (SQLite map → REAL có precision loss). Dùng `Float` cho POC; production migrate sang Postgres sẽ chuyển sang `Decimal` thật.
- KHÔNG dùng Prisma `enum` cho status field — keep type String để dễ debug + SQLite < 6.2 backward compat (mặc dù Plan 01 lock 6.6+, status string đỡ cần migration).
- `Json` type SQLite → string column. Schema dùng `String?` cho fields lưu JSON serialize manually.

**KHÔNG xóa hoặc gộp** scaffolding models (ProgramCycle/Project/Council/Contract/Report) ngay cả khi Plan 02 chỉ cần User/Organization để seed — pitfall research §4.1, §4.2 cảnh báo schema thrashing trong Phase 2-3 sẽ tốn 5-10x.
  </action>
  <verify>
    <automated>
      cd d:/Thaodnp/XTTM &amp;&amp;
      test -f prisma/schema.prisma &amp;&amp;
      grep -q 'provider = "sqlite"' prisma/schema.prisma &amp;&amp;
      grep -q 'env("DATABASE_URL")' prisma/schema.prisma &amp;&amp;
      grep -q 'model User' prisma/schema.prisma &amp;&amp;
      grep -q 'passwordHash' prisma/schema.prisma &amp;&amp;
      grep -q '@unique' prisma/schema.prisma &amp;&amp;
      grep -q 'model Organization' prisma/schema.prisma &amp;&amp;
      grep -q 'searchKey' prisma/schema.prisma &amp;&amp;
      grep -q 'model OrganizationProfile' prisma/schema.prisma &amp;&amp;
      grep -q 'model ProgramCycle' prisma/schema.prisma &amp;&amp;
      grep -q 'model Project' prisma/schema.prisma &amp;&amp;
      grep -q 'parentProjectId' prisma/schema.prisma &amp;&amp;
      grep -q 'ProjectYearLink' prisma/schema.prisma &amp;&amp;
      grep -q 'model EvaluationCouncil' prisma/schema.prisma &amp;&amp;
      grep -q 'model ScoreSheet' prisma/schema.prisma &amp;&amp;
      grep -q 'model Contract' prisma/schema.prisma &amp;&amp;
      grep -q 'model Report' prisma/schema.prisma &amp;&amp;
      grep -q 'model Attachment' prisma/schema.prisma &amp;&amp;
      grep -q 'model AuditLog' prisma/schema.prisma &amp;&amp;
      grep -q 'model Role' prisma/schema.prisma &amp;&amp;
      grep -q 'model Permission' prisma/schema.prisma &amp;&amp;
      grep -q 'model RolePermission' prisma/schema.prisma &amp;&amp;
      test -f prisma/.gitignore &amp;&amp;
      grep -q 'dev.db' prisma/.gitignore &amp;&amp;
      npx prisma format --schema=prisma/schema.prisma
    </automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` exists với exact `provider = "sqlite"` và `url = env("DATABASE_URL")`
    - 12 models defined: `User`, `Role`, `Permission`, `RolePermission`, `Organization`, `OrganizationProfile`, `ProgramCycle`, `Project`, `EvaluationCouncil`, `ScoreSheet`, `Contract`, `Report`, `Attachment`, `AuditLog` (≥12; 14 nếu count tất)
    - `User` model có exact fields: `id`, `username @unique`, `passwordHash`, `fullName`, `email`, `phone`, `role` (String), `isActive @default(true)`, `organizationId`, `createdAt`, `updatedAt`
    - `User` có `@@index([organizationId])` và `@@index([role])`
    - `Organization` có exact fields including `code @unique`, `searchKey String`, `isInvited Boolean @default(false)`
    - `Project` có `parentProjectId String?` và relation `ProjectYearLink` (per PITFALLS §4.2)
    - `Project` có `searchKey String @default("")` field
    - `Attachment` polymorphic với `entityType` + `entityId` fields + `@@index([entityType, entityId])`
    - `prisma/.gitignore` chứa `dev.db`
    - `npx prisma format` exit code 0 (schema syntactically valid)
  </acceptance_criteria>
  <done>
    Schema Prisma đầy đủ 14 models với scaffolding cho Phase 2-9. SQLite provider, env DATABASE_URL. Indexes cho performance. parentProjectId scaffolding cho đề án 2 năm. Polymorphic Attachment. Schema format pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Push schema lên SQLite + generate Prisma client</name>
  <files>prisma/dev.db, node_modules/.prisma/client/</files>
  <read_first>
    - d:/Thaodnp/XTTM/prisma/schema.prisma (Task 1 output)
    - d:/Thaodnp/XTTM/.env.local (verify DATABASE_URL="file:./dev.db")
    - d:/Thaodnp/XTTM/package.json (verify scripts db:push exists)
  </read_first>
  <action>
**[BLOCKING TASK]** Phase này CANNOT pass verification nếu task này chưa chạy thành công. Build/typecheck pass mà không push DB sẽ tạo false-positive vì Prisma Client types đến từ codegen, không phải DB live.

**Bước 1: Verify DATABASE_URL set:**
```bash
cd d:/Thaodnp/XTTM
cat .env.local | grep DATABASE_URL
# Phải thấy: DATABASE_URL="file:./dev.db"
```
Nếu chưa có, thêm vào `.env.local` (Plan 01 đã thêm — verify only).

**Bước 2: Push schema lên SQLite — đây là BLOCKING command:**
```bash
npx prisma db push --schema=prisma/schema.prisma
```
Expected output:
- "Datasource 'db': SQLite database 'dev.db' at 'file:./dev.db'"
- "🚀  Your database is now in sync with your Prisma schema. Done in XXXms"
- "✔ Generated Prisma Client (vX.X.X) to ./node_modules/@prisma/client in XXXms"

Nếu lỗi non-TTY (Windows / CI), chạy với:
```bash
npx prisma db push --schema=prisma/schema.prisma --accept-data-loss
```
Flag `--accept-data-loss` an toàn cho POC vì DB chưa có data thật (đây là first push).

**Bước 3: Verify Prisma Client generated:**
```bash
test -d node_modules/.prisma/client
test -f node_modules/@prisma/client/index.d.ts
```

**Bước 4: Verify dev.db file created:**
```bash
test -f prisma/dev.db
```

**Bước 5: Test Prisma Client load:**
```bash
node --input-type=module -e "import('@prisma/client').then(m => { const p = new m.PrismaClient(); console.log('OK', Object.keys(p).filter(k => !k.startsWith('_') && !k.startsWith('\$')).slice(0, 10)); p.\$disconnect(); })"
```
Expected: log "OK" + array model names (user, organization, project, ...).

**Bước 6: Run typecheck (now that Prisma types exist):**
```bash
npx tsc --noEmit
```
Đảm bảo `lib/prisma.ts` (Plan 01) compile đúng vì Prisma Client types đã generate.

**LƯU Ý:** Nếu push fail vì schema invalid → quay lại Task 1 fix schema, KHÔNG bypass.

**Why blocking:** Build/typecheck pass mà không push DB sẽ tạo false-positive verification. Prisma Client types đến từ codegen (chạy `prisma generate`), nhưng `lib/prisma.ts` runtime cần dev.db tồn tại để query. Plan 03 (NextAuth) cần query User table real → phải có DB pushed + seeded.
  </action>
  <verify>
    <automated>
      cd d:/Thaodnp/XTTM &amp;&amp;
      test -f prisma/dev.db &amp;&amp;
      test -d node_modules/.prisma/client &amp;&amp;
      test -f node_modules/@prisma/client/index.d.ts &amp;&amp;
      node --input-type=module -e "import('@prisma/client').then(m => { const p = new m.PrismaClient(); p.\$queryRaw\`SELECT name FROM sqlite_master WHERE type='table' AND name='User'\`.then(r => { console.log('USER_TABLE_EXISTS', r.length > 0); p.\$disconnect(); process.exit(r.length > 0 ? 0 : 1); }); })" &amp;&amp;
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `prisma/dev.db` file exists (size > 0 bytes — SQLite header + schema)
    - `node_modules/.prisma/client/` directory exists
    - `node_modules/@prisma/client/index.d.ts` exists
    - SQLite query `SELECT name FROM sqlite_master WHERE type='table' AND name='User'` returns 1 row
    - `npx tsc --noEmit` exit code 0 (Prisma Client types resolve OK trong lib/prisma.ts)
    - `git ls-files prisma/ | grep -E '\.db$'` returns empty (DB không committed)
  </acceptance_criteria>
  <done>
    Database file `prisma/dev.db` đã được tạo với toàn bộ schema. Prisma Client codegen thành công. lib/prisma.ts (Plan 01) compile + import được. Sẵn sàng cho Task 3 seed data.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Seed 8 hardcoded users với bcrypt + 5 organizations real Vietnamese names — idempotent</name>
  <files>prisma/seed.ts, prisma/seed/users.ts, prisma/seed/organizations.ts, prisma/seed/helpers.ts</files>
  <read_first>
    - d:/Thaodnp/XTTM/lib/constants.ts (HARDCODED_USERS, ORG_CODES, ROLES — verify shape)
    - d:/Thaodnp/XTTM/lib/vi-search.ts (removeDiacritics for searchKey)
    - d:/Thaodnp/XTTM/CLAUDE.md mục 5 (8 tài khoản exact passwords) + mục 8.5 (mock data realism)
    - d:/Thaodnp/XTTM/.planning/research/ARCHITECTURE.md §8 (mock data architecture, real Vietnamese names §8.3)
    - d:/Thaodnp/XTTM/.planning/research/PITFALLS.md §1.4 (honorific), §2.1 (relative dates)
    - d:/Thaodnp/XTTM/prisma/schema.prisma (Task 1 output — verify model fields)
  </read_first>
  <action>
**File 1: `prisma/seed/helpers.ts`** — Helper utilities cho seed:
```typescript
import bcrypt from 'bcryptjs';

export const BCRYPT_COST = 10;

export async function hashPassword(plain: string): Promise<string> {
  return bcrypt.hash(plain, BCRYPT_COST);
}

export function logSeedStep(name: string, count: number) {
  console.log(`✓ Seeded ${name}: ${count}`);
}
```

**File 2: `prisma/seed/organizations.ts`** — Seed 5 organizations với tên thật:
```typescript
import { PrismaClient } from '@prisma/client';
import { removeDiacritics } from '../../lib/vi-search';
import { logSeedStep } from './helpers';

type OrgSeed = {
  code: string;
  name: string;
  type: 'GOVERNMENT' | 'ASSOCIATION' | 'ENTERPRISE';
  taxCode?: string;
  address?: string;
  email?: string;
  isInvited: boolean;
};

const SEED_ORGS: OrgSeed[] = [
  {
    code: 'BO_CT',
    name: 'Bộ Công Thương',
    type: 'GOVERNMENT',
    address: '54 Hai Bà Trưng, Hoàn Kiếm, Hà Nội',
    email: 'vanthu@moit.gov.vn',
    isInvited: false,
  },
  {
    code: 'CUC_XTTM',
    name: 'Cục Xúc tiến Thương mại',
    type: 'GOVERNMENT',
    address: '20 Lý Thường Kiệt, Hoàn Kiếm, Hà Nội',
    email: 'xttm@moit.gov.vn',
    isInvited: false,
  },
  {
    code: 'VITAS',
    name: 'Hiệp hội Dệt may Việt Nam',
    type: 'ASSOCIATION',
    taxCode: '0100100100',
    address: '32 Tràng Tiền, Hoàn Kiếm, Hà Nội',
    email: 'vitas@vitas.org.vn',
    isInvited: true,
  },
  {
    code: 'LEFASO',
    name: 'Hiệp hội Da giày - Túi xách Việt Nam',
    type: 'ASSOCIATION',
    taxCode: '0100200200',
    address: '160 Hoàng Hoa Thám, Tây Hồ, Hà Nội',
    email: 'lefaso@lefaso.org.vn',
    isInvited: true,
  },
  {
    code: 'VINATEX',
    name: 'Tập đoàn Dệt May Việt Nam',
    type: 'ENTERPRISE',
    taxCode: '0100300300',
    address: '25 Bà Triệu, Hoàn Kiếm, Hà Nội',
    email: 'info@vinatex.com.vn',
    isInvited: true,
  },
];

export async function seedOrganizations(prisma: PrismaClient) {
  for (const org of SEED_ORGS) {
    await prisma.organization.upsert({
      where: { code: org.code },
      update: {
        name: org.name,
        type: org.type,
        taxCode: org.taxCode ?? null,
        address: org.address ?? null,
        email: org.email ?? null,
        isInvited: org.isInvited,
        searchKey: removeDiacritics(org.name + ' ' + org.code),
      },
      create: {
        code: org.code,
        name: org.name,
        type: org.type,
        taxCode: org.taxCode ?? null,
        address: org.address ?? null,
        email: org.email ?? null,
        isInvited: org.isInvited,
        searchKey: removeDiacritics(org.name + ' ' + org.code),
      },
    });
  }
  logSeedStep('Organizations', SEED_ORGS.length);
}
```

**File 3: `prisma/seed/users.ts`** — Seed 8 hardcoded users:
```typescript
import { PrismaClient } from '@prisma/client';
import { HARDCODED_USERS } from '../../lib/constants';
import { hashPassword, logSeedStep } from './helpers';

export async function seedUsers(prisma: PrismaClient) {
  for (const user of HARDCODED_USERS) {
    const passwordHash = await hashPassword(user.password);
    const organization = user.orgCode
      ? await prisma.organization.findUnique({ where: { code: user.orgCode } })
      : null;

    await prisma.user.upsert({
      where: { username: user.username },
      update: {
        passwordHash,
        fullName: user.fullName,
        email: user.email,
        role: user.role,
        isActive: true,
        organizationId: organization?.id ?? null,
      },
      create: {
        username: user.username,
        passwordHash,
        fullName: user.fullName,
        email: user.email,
        role: user.role,
        isActive: true,
        organizationId: organization?.id ?? null,
      },
    });
  }
  logSeedStep('Users', HARDCODED_USERS.length);
}
```

**File 4: `prisma/seed.ts`** — Entry point idempotent:
```typescript
import { PrismaClient } from '@prisma/client';
import { seedUsers } from './seed/users';
import { seedOrganizations } from './seed/organizations';

const prisma = new PrismaClient();

async function main() {
  console.log('🌱 Seeding XTTMQG database...');
  console.time('seed');

  // Order matters: orgs first (users FK organizationId), users next.
  await seedOrganizations(prisma);
  await seedUsers(prisma);

  // Smoke verify counts
  const userCount = await prisma.user.count();
  const orgCount = await prisma.organization.count();
  console.log(`📊 Total users: ${userCount}, organizations: ${orgCount}`);

  if (userCount < 8) throw new Error(`Expected 8 users, got ${userCount}`);
  if (orgCount < 5) throw new Error(`Expected ≥5 organizations, got ${orgCount}`);

  console.timeEnd('seed');
  console.log('✅ Seed complete.');
}

main()
  .catch((e) => {
    console.error('❌ Seed failed:', e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

**Bước 5: Run seed:**
```bash
cd d:/Thaodnp/XTTM
npm run db:seed
```
Expected output:
- `✓ Seeded Organizations: 5`
- `✓ Seeded Users: 8`
- `📊 Total users: 8, organizations: 5`
- `✅ Seed complete.`

**Bước 6: Verify idempotency — run seed lần 2, count vẫn = 8 / 5:**
```bash
npm run db:seed
```
Đảm bảo không có error duplicate key. Counts vẫn 8 / 5 (upsert update không create new).

**Bước 7: Smoke test bcrypt verify:**
```bash
node --input-type=module -e "
import bcrypt from 'bcryptjs';
import { PrismaClient } from '@prisma/client';
const p = new PrismaClient();
const u = await p.user.findUnique({ where: { username: 'donvi1' }, include: { organization: true } });
if (!u) { console.error('NOT_FOUND'); process.exit(1); }
const ok = await bcrypt.compare('Donvi@123', u.passwordHash);
console.log('BCRYPT_VERIFY:', ok, 'ROLE:', u.role, 'ORG:', u.organization?.name);
await p.\$disconnect();
process.exit(ok ? 0 : 1);
"
```
Expected: `BCRYPT_VERIFY: true ROLE: DONVI ORG: Hiệp hội Da giày - Túi xách Việt Nam`

**Bước 8: Smoke test cho cả 8 tài khoản:**
```bash
node --input-type=module -e "
import bcrypt from 'bcryptjs';
import { PrismaClient } from '@prisma/client';
import { HARDCODED_USERS } from './lib/constants.ts';
const p = new PrismaClient();
let allOk = true;
for (const u of HARDCODED_USERS) {
  const dbu = await p.user.findUnique({ where: { username: u.username } });
  if (!dbu) { console.error('MISSING:', u.username); allOk = false; continue; }
  const ok = await bcrypt.compare(u.password, dbu.passwordHash);
  console.log(u.username, '→', ok ? 'PASS' : 'FAIL');
  if (!ok) allOk = false;
}
await p.\$disconnect();
process.exit(allOk ? 0 : 1);
"
```
Expected: 8 lines all `PASS`.

**Lưu ý:**
- TypeScript path alias `@/lib/constants` có thể không work trong tsx runtime nếu tsconfig.json chưa enable. Plan 01 setup `paths: { "@/*": ["./*"] }` — verify tsx hỗ trợ paths qua `tsconfig-paths` hoặc dùng relative path `../../lib/constants` trong seed (đã làm ở user/org seed files).
- Bcrypt cost 10 = ~80ms/hash trên laptop modern. 8 users × 80ms = ~640ms tổng — chấp nhận được.
  </action>
  <verify>
    <automated>
      cd d:/Thaodnp/XTTM &amp;&amp;
      test -f prisma/seed.ts &amp;&amp;
      test -f prisma/seed/users.ts &amp;&amp;
      test -f prisma/seed/organizations.ts &amp;&amp;
      test -f prisma/seed/helpers.ts &amp;&amp;
      grep -q "bcrypt.hash.*10" prisma/seed/helpers.ts &amp;&amp;
      grep -q 'HARDCODED_USERS' prisma/seed/users.ts &amp;&amp;
      grep -q 'upsert' prisma/seed/users.ts &amp;&amp;
      grep -q 'upsert' prisma/seed/organizations.ts &amp;&amp;
      grep -q 'VITAS' prisma/seed/organizations.ts &amp;&amp;
      grep -q 'LEFASO' prisma/seed/organizations.ts &amp;&amp;
      grep -q 'CUC_XTTM' prisma/seed/organizations.ts &amp;&amp;
      grep -q 'BO_CT' prisma/seed/organizations.ts &amp;&amp;
      npm run db:seed &amp;&amp;
      node --input-type=module -e "import('@prisma/client').then(async m => { const p = new m.PrismaClient(); const u = await p.user.count(); const o = await p.organization.count(); await p.\$disconnect(); console.log('USERS', u, 'ORGS', o); process.exit(u === 8 &amp;&amp; o >= 5 ? 0 : 1); })" &amp;&amp;
      node --input-type=module -e "import('@prisma/client').then(async m => { const b = await import('bcryptjs'); const p = new m.PrismaClient(); const u = await p.user.findUnique({ where: { username: 'donvi1' } }); if (!u) { await p.\$disconnect(); process.exit(1); } const ok = await b.default.compare('Donvi@123', u.passwordHash); await p.\$disconnect(); console.log('BCRYPT_DONVI1', ok); process.exit(ok ? 0 : 1); })"
    </automated>
  </verify>
  <acceptance_criteria>
    - `prisma/seed.ts` is entry point, calls `seedOrganizations` then `seedUsers`, has count assertions ≥8 users / ≥5 orgs
    - `prisma/seed/helpers.ts` exports `hashPassword(plain)` using `bcrypt.hash(plain, 10)` (cost exactly 10)
    - `prisma/seed/users.ts` imports `HARDCODED_USERS` from `lib/constants.ts` (8 entries match CLAUDE.md §5)
    - `prisma/seed/organizations.ts` defines exact 5 orgs: `BO_CT`, `CUC_XTTM`, `VITAS`, `LEFASO`, `VINATEX` với `name` tiếng Việt thật (Bộ Công Thương / Cục Xúc tiến Thương mại / Hiệp hội Dệt may Việt Nam / Hiệp hội Da giày - Túi xách Việt Nam / Tập đoàn Dệt May Việt Nam)
    - Both seed files use `prisma.X.upsert({ where, update, create })` pattern (idempotent)
    - `npm run db:seed` exit code 0, output contains `Seeded Users: 8` and `Seeded Organizations: 5`
    - SQL query `SELECT COUNT(*) FROM User` returns 8
    - SQL query `SELECT COUNT(*) FROM Organization` returns 5 (or more nếu add thêm org sau)
    - Bcrypt verify `Donvi@123` against seeded `donvi1.passwordHash` returns `true`
    - Run `npm run db:seed` lần thứ 2 không tạo duplicate, counts vẫn 8/5
    - User `donvi1` có `organizationId` reference đến org `LEFASO` (per HARDCODED_USERS mapping)
    - User `banql` có `organizationId` reference đến org `CUC_XTTM`
    - User `lanhdao` có `organizationId` reference đến org `BO_CT`
    - All 8 seeded users có `isActive = true`
  </acceptance_criteria>
  <done>
    Database seeded với 8 hardcoded users (bcrypt cost 10) và 5 organizations real Vietnamese name. Idempotent. Bcrypt smoke test pass cho donvi1. AUTH-02 satisfied.
  </done>
</task>

</tasks>

<verification>
**Phase-level checks (Plan 02):**
1. `npx prisma format` exit 0 — schema syntax valid
2. `npx prisma db push` exit 0 — schema synced (Task 2 BLOCKING)
3. `npm run db:seed` exit 0 — 8 users + 5 orgs upserted
4. `npm run db:seed` lần 2 exit 0 — idempotent (counts không tăng)
5. SQL count check: `SELECT COUNT(*) FROM User = 8`, `SELECT COUNT(*) FROM Organization = 5`
6. Bcrypt verify cho 8/8 users password match
7. `git ls-files prisma/ | grep -E '\.db$'` empty (DB không commit)
8. `npx tsc --noEmit` exit 0 (Prisma Client types resolve, seed.ts compile)
</verification>

<success_criteria>
Plan 02 thành công khi:
- prisma/schema.prisma có 14 models scaffold đủ M0-M9 (User/Role/Permission/Organization/OrganizationProfile/ProgramCycle/Project với parentProjectId/EvaluationCouncil/ScoreSheet/Contract/Report/Attachment/AuditLog)
- prisma/dev.db tồn tại + Prisma Client generated [BLOCKING task 2]
- 8 hardcoded users seeded với bcrypt cost 10 hash
- 5 organizations seeded với tên Vietnamese thật
- Seed idempotent (rerun không nhân bản)
- Plan 03 có thể `prisma.user.findUnique({ where: { username }, include: { organization: true } })` và `bcrypt.compare(input, user.passwordHash)` để authenticate
- AUTH-02 requirement met
</success_criteria>

<output>
Sau hoàn thành, tạo `.planning/phases/01-m0-bootstrap-h-t-ng/01-02-prisma-schema-seed-SUMMARY.md`:
- Số models trong schema (đếm `^model ` lines)
- Output đầy đủ của `prisma db push` (database created confirmation)
- Output của 2 lần `npm run db:seed` (verify idempotent)
- 8 user passwords verified bcrypt match (PASS x8)
- Organization count + tên đầy đủ
- File size dev.db (cho biết có data thật, không empty)
- Confirmation Plan 03 ready: User table queryable, bcrypt hash valid, organization relation OK
</output>
