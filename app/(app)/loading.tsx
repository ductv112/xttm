// Default route loading skeleton cho mọi page trong (app) group.
// Phase 11-01 Task 2 (POLISH-09): skeleton-based loading thay vì spinner.

import { PageSkeleton } from '@/components/shared/PageSkeleton';

export default function Loading() {
  return <PageSkeleton />;
}
